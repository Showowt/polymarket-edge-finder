FROM node:20-alpine

WORKDIR /app

# Copy only bot files
COPY bot/package.json ./
COPY bot/scanner.js ./

# Install dependencies (if any)
RUN npm install --omit=dev 2>/dev/null || true

# Run scanner
CMD ["node", "scanner.js"]
